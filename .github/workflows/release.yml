name: Release
on:
  push:
    tags: ['v*']
jobs:
  release:
    uses: grafana/xk6/.github/workflows/extension-release.yml@103897f741839bf710a496ed68ddcafb3b751581 # v1.4.3
    permissions:
      contents: write
    with:
      go-version: "1.25.x"
      os:   '["linux","windows","darwin"]'
      arch: '["amd64","arm64"]'
      k6-version: "v2.0.0"
      xk6-version: "1.4.3"
